# =============================================================================
# Homelab Docker Compose .gitignore
# =============================================================================

# -----------------------------------------------------------------------------
# ENVIRONMENT & SECRETS
# -----------------------------------------------------------------------------
# Never commit your actual .env file (contains passwords)
.env
!.env.example

# Private keys
*.key
*.pem
*.p12
*.pfx

# WireGuard configs with private keys (generated in config/wireguard/)
# The entire wireguard config dir contains sensitive peer configs
config/wireguard/

# -----------------------------------------------------------------------------
# CONTAINER CONFIG DIRECTORIES
# -----------------------------------------------------------------------------
# These contain runtime data and should not be committed
# (they're created by docker and contain container-specific data)
config/jellyfin/
config/homeassistant/
config/transmission/
config/nextcloud/
config/syncthing/
config/grafana/

# Keep prometheus config (it's just the scrape config)
!config/prometheus/
!config/prometheus/prometheus.yml

# -----------------------------------------------------------------------------
# DOCKER
# -----------------------------------------------------------------------------
# Docker volumes (if using named volumes outside of compose)
volumes/

# -----------------------------------------------------------------------------
# EDITOR & IDE
# -----------------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~
.*.swp
.*.swo
*.code-workspace

# -----------------------------------------------------------------------------
# OS FILES
# -----------------------------------------------------------------------------
.DS_Store
.AppleDouble
Thumbs.db
Desktop.ini
.directory
.Trash-*

# -----------------------------------------------------------------------------
# LOGS & TEMP FILES
# -----------------------------------------------------------------------------
*.log
*.tmp
*.temp
*.bak
*.backup
*.orig
logs/

# -----------------------------------------------------------------------------
# DATABASES (if created locally)
# -----------------------------------------------------------------------------
*.sqlite
*.sqlite3
*.db

# -----------------------------------------------------------------------------
# CLAUDE CODE
# -----------------------------------------------------------------------------
.claude/

# -----------------------------------------------------------------------------
# NOTES
# -----------------------------------------------------------------------------
# What IS safe to commit:
#   - docker-compose.yml
#   - .env.example (template without real passwords)
#   - config/prometheus/prometheus.yml
#   - scripts/*.sh
#   - README.md
#
# What is NOT safe to commit:
#   - .env (your actual passwords)
#   - config/*/ directories (container runtime data)
#   - config/wireguard/ (contains private keys and peer configs)
# -----------------------------------------------------------------------------
