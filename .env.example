# =============================================================================
# Homelab Environment Configuration
# =============================================================================
# Copy this file to .env and update the values for your setup.
#
# Usage: cp .env.example .env && nano .env
# =============================================================================

# -----------------------------------------------------------------------------
# SYSTEM CONFIGURATION
# -----------------------------------------------------------------------------

# User/Group IDs - run 'id' to find yours (usually 1000)
PUID=1000
PGID=1000

# Timezone - find yours at: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=America/Los_Angeles

# -----------------------------------------------------------------------------
# NAS CONFIGURATION
# -----------------------------------------------------------------------------

# Your OpenMediaVault NAS IP address
NAS_IP=192.168.1.100

# -----------------------------------------------------------------------------
# WIREGUARD VPN
# -----------------------------------------------------------------------------

# Your public domain or IP for VPN connections
# If you don't have a domain, use your public IP or leave as-is and configure later
WG_SERVERURL=vpn.example.com

# WireGuard port (default: 51820)
WG_SERVERPORT=51820

# Comma-separated list of peer names to generate configs for
# Example: phone,laptop,tablet
WG_PEERS=phone,laptop

# -----------------------------------------------------------------------------
# DATABASE (shared PostgreSQL)
# -----------------------------------------------------------------------------
# Used by Nextcloud and any other services that need a database

# PostgreSQL username
POSTGRES_USER=homelab

# PostgreSQL password - CHANGE THIS!
# Generate with: openssl rand -base64 16
POSTGRES_PASSWORD=CHANGE_ME_GENERATE_RANDOM

# -----------------------------------------------------------------------------
# SERVICE PASSWORDS
# -----------------------------------------------------------------------------
# Generate secure passwords with: openssl rand -base64 16

# Transmission torrent client
TRANSMISSION_USER=admin
TRANSMISSION_PASS=CHANGE_ME_GENERATE_RANDOM

# Nextcloud admin password
NEXTCLOUD_ADMIN_PASS=CHANGE_ME_GENERATE_RANDOM

# Nextcloud trusted domains (space-separated, must be quoted)
# Add your homelab's hostname/IP and any domains you'll use
NEXTCLOUD_TRUSTED_DOMAINS="localhost homelab homelab.local 192.168.1.50"

# Grafana admin password
GRAFANA_ADMIN_PASS=CHANGE_ME_GENERATE_RANDOM

# -----------------------------------------------------------------------------
# PORKBUN DYNAMIC DNS
# -----------------------------------------------------------------------------
# Get your API keys from: https://porkbun.com/account/api

# Your Porkbun API key
PORKBUN_API_KEY=pk1_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Your Porkbun secret API key
PORKBUN_SECRET_KEY=sk1_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Domain to update (just the domain, not subdomain)
PORKBUN_DOMAIN=example.com

# Subdomain to update (use @ for root domain, or vpn, home, etc.)
PORKBUN_SUBDOMAIN=vpn

# How often to check for IP changes (in seconds, default: 300 = 5 minutes)
DDNS_INTERVAL=300

# -----------------------------------------------------------------------------
# DASHBOARD
# -----------------------------------------------------------------------------

# Optional: Override the hostname used for service links
# If not set, uses the hostname from your browser request
# DASHBOARD_HOST=homelab.local

# SSH connection info for tunnel instructions
# Used in the SSH tunnel setup guides
SSH_HOST=homelab
SSH_USER=admin
